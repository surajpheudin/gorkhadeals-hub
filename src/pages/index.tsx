import { Box } from "@chakra-ui/react";
import TopBar from "@components/Home/TopBar";
const Home = () => {
    return (
        <Box p={4}>
            <TopBar />
        </Box>
    );
};
export default Home;
